import React,{useEffect,useState}from'react';import{api}from'../services/api';
export default function Employer(){const[apps,setApps]=useState([]),[jobs,setJobs]=useState([]);useEffect(()=>{api.get('/admin/applications').then(r=>setApps(r.data));api.get('/admin/jobs').then(r=>setJobs(r.data))},[]);return <div className="max-w-7xl mx-auto px-4 py-10"><h1 className="text-4xl font-black mb-6">Панель работодателя</h1><div className="grid lg:grid-cols-2 gap-6"><div className="p-6 bg-white dark:bg-slate-900 rounded-3xl shadow"><h2 className="font-black text-2xl mb-4">Мои вакансии</h2>{jobs.map(j=><p key={j.id} className="p-3 bg-slate-100 dark:bg-slate-800 rounded-xl mb-2">{j.title} — {j.country}</p>)}</div><div className="p-6 bg-white dark:bg-slate-900 rounded-3xl shadow"><h2 className="font-black text-2xl mb-4">Заявки кандидатов</h2>{apps.map(a=><p key={a.id} className="p-3 bg-slate-100 dark:bg-slate-800 rounded-xl mb-2">{a.user_name} → {a.job_title}</p>)}</div></div></div>}
