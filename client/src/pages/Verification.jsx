import React,{useState}from'react';import{api}from'../services/api';import{useAuth}from'../context/AuthContext';
export default function Verification(){const{profile,load}=useAuth();const[form,setForm]=useState({full_name:profile?.full_name||'',phone:profile?.phone||'',country:profile?.country||''}),[file,setFile]=useState(null),[msg,setMsg]=useState('');async function submit(e){e.preventDefault();const fd=new FormData();Object.entries(form).forEach(([k,v])=>fd.append(k,v));if(file)fd.append('document',file);const{data}=await api.post('/profile',fd);setMsg(data.message);load()}return <div className="max-w-3xl mx-auto px-4 py-10"><h1 className="text-4xl font-black mb-4">Верификация</h1><p className="mb-5">Текущий статус: <b className="text-blue-600">{profile?.verification_status}</b></p><form onSubmit={submit} className="bg-white dark:bg-slate-900 p-8 rounded-3xl shadow space-y-4"><input className="w-full p-3 rounded-xl bg-slate-100 dark:bg-slate-800" placeholder="ФИО" value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/><input className="w-full p-3 rounded-xl bg-slate-100 dark:bg-slate-800" placeholder="Телефон" value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/><input className="w-full p-3 rounded-xl bg-slate-100 dark:bg-slate-800" placeholder="Страна" value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/><input type="file" onChange={e=>setFile(e.target.files[0])}/><button className="w-full p-3 rounded-xl bg-blue-600 text-white font-bold">Отправить на проверку</button>{msg&&<p className="text-green-600">{msg}</p>}</form></div>}
